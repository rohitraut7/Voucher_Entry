import React from 'react';
import Form from '@/components/form/forms';

const page = () => {
    return (
        <div>
            <Form />
            
        </div>
    );
};

export default page;